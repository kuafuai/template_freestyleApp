const express = require('express');
const app = express();
const port = 3000;

// TODO: Implement the server code here

app.listen(port, () => {
  console.log(`Server is running on port ${port}`);
});