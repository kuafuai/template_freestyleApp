{
  "name": "Outlook Plugin",
  "version": "1.0",
  "description": "A plugin for sending current email as attachment to a specified Outlook mailbox. This plugin allows users to easily attach the current email as a file to a specified Outlook mailbox.",
  "manifest_version": 2,
  "background": {
    "scripts": ["background.js"],
    "persistent": false
  },
  "permissions": [
    "https://outlook.com/",
    "activeTab"
  ],
  "browser_action": {
    "default_icon": "icon.png",
    "default_popup": "popup.html"
  }
}