// The application's entry file, used to initialize the application.

// Initialize the application
import Vue from 'vue'
import App from './App.vue'

new Vue({
  render: h => h(App)
}).$mount('#app')
